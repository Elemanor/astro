---
export interface Props {
  href: string;
  text: string;
  class?: string;
}

const { href, text, class: className = '' } = Astro.props;
---

<a 
  href={href} 
  class={`text-blue-600 hover:text-blue-800 underline font-medium transition-colors ${className}`}
>
  {text} →
</a>